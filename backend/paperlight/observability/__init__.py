"""Observability — Sentry errors + Langfuse LLM traces + request correlation (S15).

All integrations are env-gated: with no keys set every entry point is a no-op so
tests and local dev run untouched (PRD §7.9 — Phase 1 covers trace visualization).
"""
